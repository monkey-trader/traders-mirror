name: Lint (disabled)

on:
  workflow_dispatch: {}

jobs:
  noop:
    name: Disabled noop job
    runs-on: ubuntu-latest
    steps:
      - run: echo "Lint workflow disabled; CI handles lint on push/PR."
